<Window x:Class="Scrummable.AddBugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Bug to Dashboard" Height="200" Width="300">
    <Grid Background="#222222" FocusManager.FocusedElement="{Binding ElementName=BugIdTB}">
        <StackPanel Orientation="Vertical">
            <TextBlock FontFamily="Segoe UI" FontWeight="Light" FontSize="24" Foreground="#ffffff">Enter Bug Id:</TextBlock>
            <TextBox x:Name="BugIdTB" Margin="20 20 20 0" FontSize="24" FontFamily="Segoe UI" FontWeight="Light" Foreground="#262626" TabIndex="1"></TextBox>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AddBugIdBtn" Margin="20" Padding="5" FontSize="18" FontFamily="Segoe UI" FontWeight="Light" Foreground="#262626" Content="Lookup Bug" VerticalAlignment="Center" HorizontalAlignment="Right" Click="AddUserNameBtn_Click"></Button>
                <Button x:Name="CancelBtn" Margin="20" Padding="5" FontSize="18" FontFamily="Segoe UI" FontWeight="Light" Foreground="#262626" Content="Cancel" VerticalAlignment="Center" HorizontalAlignment="Right" Click="CancelBtn_Click"></Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
